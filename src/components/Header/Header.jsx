import React from 'react';
import s from './Header.module.css';

const Header = () => {
    return (
        <div className={s.header}>
            Поиск документов
        </div>
    );
}

export default Header;